不熟练

```python
import string

letters = string.ascii_letters					#所有的英文字母，包括大小写
lowercase = string.ascii_lowercase				#所有的小写字母
uppercase = string.ascii_uppercase				#所有的大写字母
digits = string.digits					

推导式
读取或操作（如读取单行数据，多行数据，修改文件内容，增加文件数据，将文件内的数据做主力）各类文件（文本文件，excel等），得到想要的结果（将读取到的数据如何处理，或者做出什么样的表，又或者保存，将处理后的数据建立一个新文件）---------总结，所有和操作读取文件相关的都不熟练

集合未讲（之前学过，若讲的东西无更为深入或特别的，可跳过）




```



不会用

```
zip()
enumerate()
```













自我感觉缺乏

```
使用一些更为简便的方法（如直接既取模又取余了的方法，一些已经实现了的方法--不用自己写，直接使用就行。      辅助型方法，可以使解决问题的速度更快，写的代码更少，如zip,enumerate）

框架的理解，使用，使用框架的内有的方法，如获取数据，获取到的数据做出处理，交互过程中可能需要的其他辅助模块的使用（如json格式的解码和转码，数据库连接使用工具[如调用的方法，传什么参，得到的返回值是什么样]） 等问题        ---感觉可能原因   这块未学完项目实战部分（刚学一节半的课），未系统连续的跟随练习，在练习中遇见的环境问题解决耗时较长。
```

